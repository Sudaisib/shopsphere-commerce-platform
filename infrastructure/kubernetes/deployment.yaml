apiVersion: apps/v1
kind: Deployment

metadata:
  name: shopsphere

spec:
  replicas: 2

  selector:
    matchLabels:
      app: shopsphere

  template:
    metadata:
      labels:
        app: shopsphere

    spec:
      containers:
        - name: shopsphere
          image: shopsphere:1.0
          